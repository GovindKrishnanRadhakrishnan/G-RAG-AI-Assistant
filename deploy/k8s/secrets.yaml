apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
  namespace: rag-namespace
type: Opaque
data:
  # Empty by default (add base64 values in production)
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
