# Archive

Parked code that is **not** part of the active G-RAG application.

| Path | Why it is here |
|------|----------------|
| `streamlit_app.py` | Duplicate UI — G-RAG uses FastAPI + `templates/index.html` |
| `agents/` | Research-agent / planning path removed from the API |
| `tools/` | arXiv / Scholar / paper tools used only by the archived agent |
| `scratch_*`, `start_*`, `*_out.txt`, `READ.md` | Dev scratch and temporary output |

These files are kept for reference and can be restored later if needed.
They are not imported by the running app.
